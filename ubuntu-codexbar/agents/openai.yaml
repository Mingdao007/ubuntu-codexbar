interface:
  display_name: "Ubuntu Codexbar"
  short_description: "Inspect Codex saved profiles, root auth mismatches, and usage snapshots on Ubuntu"
  default_prompt: "Use $ubuntu-codexbar to inspect, switch, or debug saved Codex login profiles, root-auth mismatches, and usage snapshots on Ubuntu with the local codexbar CLI."
